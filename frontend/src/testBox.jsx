import React from 'react';
import { Box } from '@mui/material';

export default function TestBox() {
    return (
        <Box sx={{ minHeight: '100vh', backgroundColor: 'blue' }}>
            Test Box Component
        </Box>
    );
}